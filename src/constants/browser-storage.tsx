import { PROJECT_NAME } from "@/configs/env";

export const LOCAL_STORAGE_KEYS = {
  AUTH_USER_INFO: `${PROJECT_NAME}-auth-user-info`,
  AUTH_INFO: `${PROJECT_NAME}-auth-info`,
  REMEMBER_ME_INFO: `${PROJECT_NAME}-remember-info`,
};
